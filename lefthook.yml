pre-commit:
  parallel: true
  commands:
    prettier-frontend:
      root: "frontend/"
      run: pnpm prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
    prettier-cms:
      root: "cms/"
      run: pnpm prettier --write --ignore-unknown {staged_files}
      stage_fixed: true

